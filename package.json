{
  "name": "physique_chimie",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "deno run -A node_modules/.bin/vite",
    "build": "deno run -A node_modules/.bin/vite build",
    "preview": "deno run -A node_modules/.bin/vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "katex": "^0.16.9",
    "three": "^0.181.0"
  }
}
